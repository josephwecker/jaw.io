\---
layout: main
\---
%nav#post-index
  %ul#posts
    {% for post in site.posts %}
    %li
      %time(datetime="{{ post.date }}") {{ post.date | date_to_string }}
      %a(href="{{ post.url }}") / {{ post.title }} /
    {% endfor %}
%nav#projects
